pipeline{
    agent   any
    environment{
        name="Ali"
        Age=19
    }
    stages{
        stage("Test"){
            steps{
                sh'''
                    echo "Test"
                    ls
                    echo "Name: ${name}"
                    echo "Age: ${Age}"
                '''
            }
            
        }
    }
    post{
        always{
            echo "========always========"
        }
        success{
            echo "========pipeline executed successfully ========"
        }
        failure{
            echo "========pipeline execution failed========"
        }
    }
}